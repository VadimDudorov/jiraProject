spring:
  datasource:
    url: jdbc:mysql://localhost:3306/students
    username: root
    password: 15111998090404
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: MySql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true